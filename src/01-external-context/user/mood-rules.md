# Mood Rules

- 这个文件用于描述“情绪到编排结果”的映射规则结构，而不是填写固定情绪案例。
- 这里应该说明：输入主题如何影响选歌、主持词、串场词、TTS 风格和节目节奏。
- 推荐把规则写成可扩展的映射约束，例如“什么类型的关键词会影响 provider、文案语气、过门强度”。
- `mood-rules.md` 的目标是让主题解析有统一规范，方便后续把自然语言规则升级成结构化配置。
